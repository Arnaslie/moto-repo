@AGENTS.md

Avoid comment bloat, only add helpful one-liners for complex systems and components.
Make sure lint passes as a pre-commit hook and build passes on pushes to git remote.

Follow principles like KISS, DRY